<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme for the app with DarkActionBar (for other activities) -->
    <style name="Theme.WallSet" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary branding -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary branding -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/background</item>
        <item name="colorSurface">@color/background</item>
        <item name="colorOnSurface">@color/on_background</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">?attr/colorPrimary</item>

        <!-- Dark theme control -->
        <item name="android:forceDarkAllowed">true</item>

        <!-- Action Bar -->
        <item name="actionBarStyle">@style/WallSet.ActionBar</item>
    </style>

    <!-- Custom Theme for MainActivity (No Action Bar) -->
    <style name="Theme.MainActivity" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary branding -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary branding -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/background</item>
        <item name="colorSurface">@color/background</item>
        <item name="colorOnSurface">@color/on_background</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">?attr/colorPrimary</item>

        <!-- Dark theme control -->
        <item name="android:forceDarkAllowed">true</item>

        <!-- Action Bar -->
        <item name="actionBarStyle">@style/WallSet.ActionBar</item>
    </style>

    <!-- Custom ActionBar Style (for other activities with action bar) -->
    <style name="WallSet.ActionBar" parent="Widget.MaterialComponents.ActionBar.Solid">
        <item name="elevation">0dp</item>
    </style>
</resources>
