<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- Resim görüntüleme alanı -->
    <ImageView
        android:id="@+id/wallpaperImageView"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"/>

    <!-- Favoriye ekle/çıkart butonu -->
    <Button
        android:id="@+id/favoriteButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Favorilere Ekle"
        android:layout_marginTop="8dp"/>

    <!-- Favorilerden çıkar butonu (sadece Favoriler sayfasında görünmeli) -->
    <Button
        android:id="@+id/removeFromFavoritesButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Favorilerden Çıkar"
        android:visibility="gone"
        android:layout_marginTop="8dp"/>
</LinearLayout>
